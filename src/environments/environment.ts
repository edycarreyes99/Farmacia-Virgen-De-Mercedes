// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: 'AIzaSyCFvUoPFrNXl985CR4P4WNb2PdauvmNPFk',
    authDomain: 'farmaciavirgendemercedes.firebaseapp.com',
    databaseURL: 'https://farmaciavirgendemercedes.firebaseio.com',
    projectId: 'farmaciavirgendemercedes',
    storageBucket: 'farmaciavirgendemercedes.appspot.com',
    messagingSenderId: '653745224968',
    appId: '1:653745224968:web:5a491fd177e6fe93'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
